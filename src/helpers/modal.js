/**
 * @typedef {Object} ModalState
 * @property {boolean} isOpen
 * @property {string|null} modalName
 */

/** @type {ModalState} */
export const initialModalState = {
  isOpen: false,
  modalName: null,
};
